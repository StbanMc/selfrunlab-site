# selfrunlab.com

Sitio público de SelfRun Lab, laboratorio de empresas que se operan solas. Publicamos lo que medimos.

Página estática, sin cookies ni rastreadores, en español (`/`) y en inglés (`/en/`). Guía para agentes en `llms.txt`; la oferta en `offer.json`; la página en texto plano en `index.md`.

---

The public site of SelfRun Lab, the lab for companies that run themselves. We publish what we measure.

A static page with no cookies or trackers, in Spanish (`/`) and English (`/en/`). Guide for agents in `en/llms.txt`; the offer in `en/offer.json`; the page in plain text in `en/index.md`.
